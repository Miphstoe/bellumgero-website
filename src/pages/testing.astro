---
const pageTitle = "Bellum Gero | Testing Page";

const quickLinks = [
  {
    label: "Play Now",
    href: "http://15.204.163.178/tre/BellumGero%20Launcher_0.1.0_x64-setup.exe",
    kind: "primary"
  },
  {
    label: "Skill Calculator",
    href: "https://www.bellumgero.net/skills.html",
    kind: "ghost"
  },
  { label: "Discord", href: "https://discord.gg/jSsR2hQZGw", kind: "ghost" },
  {
    label: "Facebook Group",
    href: "https://www.facebook.com/groups/2255603491594451",
    kind: "ghost"
  },
  { label: "Twitter / X", href: "https://twitter.com/BellumGeroSWG", kind: "ghost" }
];

const highlights = [
  "10x XP across all professions",
  "Builder-friendly housing and city tools",
  "Random world bosses and endgame cave content",
  "Custom Jedi path and branch unlock system",
  "Galaxy-wide grouping and major quality-of-life updates"
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{pageTitle}</title>
    <meta
      name="description"
      content="Testing page for the Bellum Gero Astro site rebuild."
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Exo+2:wght@400;500;700&family=Orbitron:wght@600;800&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --space-0: #030713;
        --space-1: #07142c;
        --space-2: #0f2a52;
        --star: #d8e7ff;
        --accent: #f28c1b;
        --accent-soft: rgba(242, 140, 27, 0.18);
        --text: #eff5ff;
        --muted: #9cb1d3;
        --panel: rgba(7, 18, 41, 0.8);
        --border: rgba(216, 231, 255, 0.2);
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        min-height: 100vh;
        font-family: "Exo 2", "Trebuchet MS", sans-serif;
        color: var(--text);
        background:
          radial-gradient(circle at 20% 10%, rgba(46, 89, 156, 0.38), transparent 42%),
          radial-gradient(circle at 85% 20%, rgba(19, 55, 118, 0.4), transparent 36%),
          radial-gradient(circle at 50% 80%, rgba(12, 38, 80, 0.28), transparent 42%),
          var(--space-0);
      }

      body::before {
        content: "";
        position: fixed;
        inset: 0;
        pointer-events: none;
        background-image:
          radial-gradient(2px 2px at 14% 26%, rgba(216, 231, 255, 0.65), transparent),
          radial-gradient(1.5px 1.5px at 77% 33%, rgba(216, 231, 255, 0.55), transparent),
          radial-gradient(1.8px 1.8px at 60% 72%, rgba(216, 231, 255, 0.48), transparent),
          radial-gradient(1.2px 1.2px at 24% 84%, rgba(216, 231, 255, 0.52), transparent);
      }

      .wrap {
        width: min(1080px, 92vw);
        margin: 0 auto;
        padding: 2rem 0 3rem;
      }

      .topbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 1rem;
      }

      .brand {
        font-family: "Orbitron", sans-serif;
        font-weight: 800;
        letter-spacing: 0.08em;
        color: var(--accent);
      }

      .status {
        border: 1px solid var(--border);
        border-radius: 999px;
        padding: 0.35rem 0.75rem;
        color: var(--muted);
        font-size: 0.85rem;
        background: rgba(3, 10, 25, 0.5);
      }

      .hero {
        margin-top: 1.2rem;
        padding: 1.6rem;
        border-radius: 1rem;
        border: 1px solid var(--border);
        background: linear-gradient(145deg, rgba(3, 10, 25, 0.86), var(--panel));
        box-shadow: 0 20px 50px rgba(0, 0, 0, 0.42);
      }

      h1 {
        font-family: "Orbitron", sans-serif;
        margin: 0;
        font-size: clamp(1.6rem, 3vw, 2.4rem);
        line-height: 1.15;
      }

      h1 span {
        color: var(--accent);
      }

      .subtitle {
        margin: 0.9rem 0 1.2rem;
        max-width: 64ch;
        color: var(--muted);
      }

      .actions {
        display: flex;
        flex-wrap: wrap;
        gap: 0.6rem;
      }

      .btn {
        text-decoration: none;
        border-radius: 999px;
        padding: 0.55rem 0.95rem;
        border: 1px solid transparent;
        font-weight: 600;
        font-size: 0.9rem;
      }

      .btn-primary {
        color: #111;
        background: linear-gradient(135deg, #f28c1b, #ffb257);
      }

      .btn-ghost {
        color: var(--text);
        border-color: var(--border);
        background: rgba(5, 12, 30, 0.62);
      }

      .grid {
        margin-top: 1.1rem;
        display: grid;
        gap: 1rem;
        grid-template-columns: 1.4fr 1fr;
      }

      .panel {
        border: 1px solid var(--border);
        border-radius: 0.85rem;
        padding: 1rem;
        background: rgba(6, 15, 34, 0.72);
      }

      .panel h2 {
        margin: 0 0 0.55rem;
        font-size: 1.05rem;
        color: var(--accent);
      }

      .panel p,
      .panel li {
        color: var(--muted);
      }

      ul {
        margin: 0;
        padding-left: 1.1rem;
        display: grid;
        gap: 0.35rem;
      }

      .event {
        border-left: 4px solid var(--accent);
        padding-left: 0.75rem;
      }

      .event strong {
        color: var(--text);
      }

      footer {
        margin-top: 1.5rem;
        color: var(--muted);
        font-size: 0.86rem;
        text-align: center;
      }

      @media (max-width: 850px) {
        .grid {
          grid-template-columns: 1fr;
        }

        .topbar {
          flex-direction: column;
          align-items: flex-start;
        }
      }
    </style>
  </head>
  <body>
    <div class="wrap">
      <header class="topbar">
        <div class="brand">BELLUM GERO</div>
        <div class="status">Star Wars Galaxies Classic+ Test Build</div>
      </header>

      <main>
        <section class="hero">
          <h1>Welcome to <span>Bellum Gero</span></h1>
          <p class="subtitle">
            A Star Wars Galaxies server focused on fast progression, strong support for
            builders and city mayors, and challenging endgame content.
          </p>

          <div class="actions">
            {
              quickLinks.map((link) => (
                <a
                  class={`btn ${link.kind === "primary" ? "btn-primary" : "btn-ghost"}`}
                  href={link.href}
                  target={link.href.startsWith("http") ? "_blank" : undefined}
                  rel={link.href.startsWith("http") ? "noopener" : undefined}
                >
                  {link.label}
                </a>
              ))
            }
          </div>
        </section>

        <section class="grid">
          <article class="panel">
            <h2>Current Feature Highlights</h2>
            <ul>
              {highlights.map((item) => <li>{item}</li>)}
            </ul>
          </article>

          <aside class="panel">
            <h2>Upcoming Event</h2>
            <p class="event">
              <strong>Battle for Woolamander Crossing</strong><br />
              Sunday, February 22, 2026 at 2:00 PM EST<br />
              Location: Woolamander Crossing, Yavin 4
            </p>
            <p>
              Grouping starts around 1:00 PM EST. Buffs and resurrections are provided
              on-site.
            </p>
          </aside>

          <article class="panel" style="grid-column: 1 / -1;">
            <h2>Latest Patch Snapshot (February 17, 2026)</h2>
            <p>
              Added new creatures to improve resource availability, expanded utility
              rewards, and increased world boss rewards to 3 items per participant.
              This update also includes city decoration additions and Jedi progression
              improvements.
            </p>
          </article>
        </section>
      </main>

      <footer>
        Testing page in Astro format. Next step: split into reusable components and hook
        live server status.
      </footer>
    </div>
  </body>
</html>
